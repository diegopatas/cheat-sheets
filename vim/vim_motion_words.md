```sh
w	    # word
W       # jump forwards to the start of a word (punctuation)
b	    # back
B       # jump backwards to the start of a word (punctuation)
e		# end
E       # jump forwards to the end of a word (punctuation)
ge      # jump backwards to the end of a word
gE      # jump backwards to the end of a word
```