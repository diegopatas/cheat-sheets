```bash
/pattern         # search pattern /ft_myfunc
?pattern         # search backward
\vpattern        # very magic (non-alph)
n                # find next match
N                # find before match
```