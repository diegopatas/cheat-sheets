```shell
git checkout -b dev # create and switch to branch
git add . && git commit -m "feat: message" # add all and commit
git checkout main # switch to main
git merge dev # merge dev to current branch
git checkout -d dev # delete branch
```