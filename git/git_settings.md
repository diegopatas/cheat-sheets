```shell
git config --global user.name "my_name" # set user scope name
git config --global user.email "my_name@email.com" # set email
git config --global core.editor vim # set editor
git config --list # list
```