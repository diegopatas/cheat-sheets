```shell
git fetch origin # fetch to remote branch
git fetch --all 
git checkout -b main origin/main # create and track
git push origin main # push main to remote
git push -u origin main # push upstream
```