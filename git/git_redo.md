```shell
git rm --cached <file> # removes version ctrl
git reset <file> # unstages the file, preserve
git reset <commit> # undoes all commits after # 
# preserve local
git revert -n <sha> # revert a commit
```