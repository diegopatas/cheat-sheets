```shell
git stash # temp stores all mod track files
git stash save "message" # save local to new stash
git stash pop # restore recently
git stash list # list
git stash show # show the changes
git stash drop # discard recently
```