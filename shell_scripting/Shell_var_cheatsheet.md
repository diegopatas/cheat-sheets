```shell
my_var="Diego" # user-defined var
echo $my_var   # using var
echo $HOME     # env var HOME

my_arr=(one two three four) # array declaring
echo ${my_arr[1]} # using array
unset my_arr[0]  # delete one
unset my_arr     # delete all
```
